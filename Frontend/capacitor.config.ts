import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.sheeper.misterblog',
  appName: 'Mister-Blog',
  webDir: 'dist/Mister-Blog/browser',
  bundledWebRuntime: false
};

export default config;
